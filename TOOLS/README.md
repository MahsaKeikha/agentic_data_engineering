# F33 Tools

Executable data-engineering tools live in [`src/tools/`](../src/tools/).

The tool layer supports schema/contract inspection, lineage artifacts, data-quality checks, transformation analysis, and reliability evidence. Tools perform deterministic operations; agents invoke them through skills.

See [`src/tools/domain_tools.py`](../src/tools/domain_tools.py).